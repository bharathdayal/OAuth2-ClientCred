spring.application.name=AuthorizationServer
server.port=9000

# The issuer your tokens will contain (and that resource servers will trust)
spring.security.oauth2.authorizationserver.issuer=http://localhost:9000

# Optional: helpful logs while wiring things up
logging.level.org.springframework.security=INFO
management.endpoints.web.exposure.include=health,info
